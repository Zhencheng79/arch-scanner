# viewer.html 连线优化 + 图例更新 (v0.1.44)

## 工作目录
/Users/zhencheng/projects/hermes-3d-panorama/
当前分支: feature/node-spread
文件: packages/3d-monitor/viewer.html (4421行)

## 重要约束
- 保留所有现有内联JS库不变（THREE.js, OrbitControls, CSS2DRenderer）
- 不要拆分成多文件，viewer.html 保持单文件内联
- 不要删除 console.log ——替换为空行
- 修改前先备份原文件

## 改动需求

### 1. 连线优化（重点）
当前节点驱动模式下连线"往天上飞再折回来"，看起来不自然。
需要调整连线计算的参数或逻辑，让连线更自然：
- 减少不必要的向上翻越（减少 baseElev 的值，或让中间控制点更低）
- 连线尽量贴近节点平面
- 检查连线端点是否对准节点中心（疑似偏离）
- 可以借鉴力导向版本的连线绘制方式（如果分支上有的话）

### 2. 左上角状态栏
当前第94行：
`<div id="stats"><b id="fps">0</b> FPS · <b id="nodeCount">0</b> nodes · <b id="edgeCount">0</b> edges · 层数: <b id="layerCount">5</b>层</div>`

去掉 "· 层数: 5层" 这部分（"层数"信息在当前没有价值）

### 3. 左下角图例
当前第110行：
`排布: Y轴松散分布 | X轴业务领域参考 | Z轴内外边界参考`

改为：
`排布: Y轴自由散布 | X轴业务领域参考 | Z轴内外边界参考`

### 4. 连线端点检查
检查每条 connection 的起点终点位置是否精确对应节点中心位置。
如果偏离，修复为精确对齐。

### 5. 版本号更新
将 viewer.html 中所有的 "v0.1.44" 字样保持不变（已经是v0.1.44）。
如果有 "v0.1.43" 或更旧版本号字符串，更新为 "v0.1.44"。

## 验收标准
- 连线走向自然，不往天上飞
- 连线端点对齐节点中心
- 左上角无"层数"信息
- 左下角排布描述为"Y轴自由散布"
- 0 JS 报错
- 浏览器打开后节点连线正常显示
