# v0.1.40-node-spread.14: 恢复色彩表现力 + 修正

## 问题
v13 把 resolveLayerColor 改成 resolveModuleColor 后，节点颜色全变了。
之前的5层鲜明配色（蓝/青/绿/黄/红）没了，视觉层次感下降。

## 方案
不改颜色体系，在已有基础上加信息：
1. 节点颜色 → 恢复为层颜色（原有的5层配色）
2. 模块归属 → 节点加模块色边框或小标签
3. 背景框 → 用模块颜色不冲突
4. 详情面板 → 已经加了功能说明，保留
5. 箭头 → 保持v13的加粗配置

## 文件
packages/3d-monitor/viewer.html
