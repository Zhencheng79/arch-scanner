# v15: 表达力+间距+模块聚类+同步force-layout

## 分支
feature/node-spread

## 修复
1. **详情面板加架构信息**：在功能说明下方加"架构角色"行，显示该模块在管道架构中的位置
2. **单节点模块加小台面**：去掉 info.nodeCount < 2 限制，改为 nodeCount >= 1
3. **背景框加粗**：边框宽度 1→2，透明度 0.12→0.18
4. **间距再缩**：MAX_PER_ROW 12→16
